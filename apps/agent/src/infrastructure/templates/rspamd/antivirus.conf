# ClamAV integration for Rspamd - Generated by ServerFlow
clamav {
    scan_mime_parts = {{ scan_mime_parts | default:true }};
    servers = "{{ clamav_socket | default:/var/run/clamav/clamd.sock }}";
    symbol = "CLAM_VIRUS";
    patterns {
        JUST_EICAR = "^Eicar-Test-Signature$";
    }
}
