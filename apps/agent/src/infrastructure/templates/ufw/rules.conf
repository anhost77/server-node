# UFW Rules - Generated by ServerFlow
# Applied automatically during installation

# Default policies
default deny incoming
default allow outgoing

# Allowed services
{{#each allowed_ports}}
allow {{ port }}
{{/each}}
